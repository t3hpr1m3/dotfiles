set columns=100
set lines=60
"set guifont=Consolas\ 9 
source ~/.vim_font
imap \timestamp <C-R>=strftime("%a %b %d %T %Z %Y")
"colorscheme koehler_status
"colorscheme jellybeans 
source ~/.vim_colors
"au VimEnter * NERDTree
